public class javaOperatorler 
{
    public static void main(String[] args)
    {
        // java da operatörler
        
        // aritmetik operatörler

        // a + b toplama
        // a - b çıkarma
        // a * b çarpma
        // a / b bölme
        // a % b mod alma (kalan alma)
        // a ++ 1 arttırma
        // a -- 1 azaltma 
        
        // ilişkisel (mantıksal) operatörler

        // a == b eşitmidir
        // a != b farklımıdır
        // a < b küçükmüdür
        // a > b büyükmüdür
        // a <= b küçük eşit midir
        // a >= b büyük eşitmidir

        // bağlaçlar

        // a && b ve
        // a || b veya
        // !(a && b) ve değil

        // koşul operatörü

        // a = (b == 1) ? 1 : 0

        // koşul operatörü örnek
        int b;
        b = 5;
        boolean a = (b == 1) ? true :false; // b 1 değerine eşit olursa true değerini döndürür
        System.out.println(a); // b eğer b değerinden farklı bir değere sahipse false değerini döndürür
        // not : bu kullanım sadece true false değerlerini döndürmez baştaki boolean ifadesinin yerine string bir ifade
        // yazılarak 2 ihtimal için de string bir değer döndürülmesi sağlanabilir ya da herhangi bir veri tipi için
        

    }    
}
